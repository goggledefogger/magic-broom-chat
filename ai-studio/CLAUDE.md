# AI Studio Version — Magic Brooms

This is the Google AI Studio version of Magic Brooms, built using Google's AI Studio as the primary AI-assisted development tool.

## Devlog Rule

After completing any major milestone (planning, implementation, testing, etc.), **automatically** create a devlog entry in `../devlog/` and update `../devlog/README.md`. Use the prefix `ai-studio-NNN` (e.g., `ai-studio-001-kickoff.md`). Check existing `ai-studio-*` entries in `../devlog/` first to pick the next number. See the root `CLAUDE.md` for content details.
